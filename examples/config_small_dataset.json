{
  "description": "Configuration template for small datasets (<10,000 records)",
  "input_entities_file": "path/to/entities.csv",
  "input_codes_file": "path/to/codes.csv",
  "output_directory": "./output",
  "fuzzy_thresholds": [95, 90],
  "district_code_mapping": {},
  "enable_district_code_mapping": true,
  "district_fuzzy_threshold": 85,
  "chunk_size": null,
  "log_level": "INFO",
  "log_file": null,
  "notes": [
    "No chunk processing needed for small datasets",
    "Use INFO log level for standard output",
    "Two fuzzy thresholds provide good balance of speed and accuracy",
    "District code mapping enabled by default with 85% fuzzy threshold"
  ]
}
